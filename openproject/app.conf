# OpenProject Application Metadata
# Used by sixnet deploy tooling

# App identity
APP_NAME=openproject
APP_SUBDOMAIN=projects
APP_ACCESS=vpn

# Access control
APP_SSO=false           # OpenProject Community doesn't support OIDC

# Internal service (for Caddy reverse proxy)
# Container name on sixnet network
# Description
APP_DESCRIPTION="Project management and collaboration"
